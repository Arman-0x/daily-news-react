import React, { Component } from 'react'
import PropTypes from 'prop-types'

export class NewsItem extends Component {
  static propTypes = {

  }

  render() {
    return (
      <div>
        i will chage once it will change all components
      </div>
    )
  }
}

export default NewsItem
